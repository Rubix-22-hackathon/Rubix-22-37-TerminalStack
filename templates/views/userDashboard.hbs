{{>userNavbar}}

    
    <Section class="Profile">
        <div class="topSec"> 
            <div class="profimgdiv">
                <img class="profimg" src="image/male-profile.jpg"/>
            </div>
            <h1 class="welcome">Welcome {{name}}</h1>
        </div>
        <div class="bottomsec">
            <button class="accordion">User Information</button>
            <div class="panel">
               <h1>Name : {{name}}</h1>
                <h1>Email : {{email}}</h1>
            </div>

            <button class="accordion">My Appointments</button>
            <div class="panel">
                {{#each rows}}
                <a href="/bookappointment?email={{this.dremail}}">{{this.dremail}}</a>
                {{/each}}
                {{!-- {{#each rows}}
                <h1>{{this.patientemail}} Wants to connect</h1>
                <form action="/accpetConnectReq?email={{this.patientemail}}" method="post">
                    <button>Accept request</button>
                </form>
                {{/each}} --}}
            </div>

            <button class="accordion">User History</button>
            <div class="panel">
                <p>Lorem ipsum...</p>
            </div>
        </div>
        </Section>
{{>footer}}    
<script src="js/userDashboard.js"></script>